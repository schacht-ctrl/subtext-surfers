# Static site – no build step needed
[build]
  publish = "."

# Never cache the game itself; always fresh after a deploy
[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
